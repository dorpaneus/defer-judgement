# <Problem name>

**Source:** <link to LeetCode / HackerRank / book / wherever, or "self-posed">
**Difficulty:** <easy | medium | hard | n/a>
**Tags:** `<tag1>`, `<tag2>`, `<tag3>`
**Date solved:** YYYY-MM-DD

---

## Problem

Restate the problem in your own words in 2–4 sentences. If you can't, you don't understand it yet.

**Input:** <shape and constraints>
**Output:** <shape>
**Constraints worth noting:** <e.g. n ≤ 10⁵, values can be negative, array may be empty>

## Examples

```
Input:  ...
Output: ...
Why:    ...
```

## Approach

A few lines on the strategy. *Why* this approach — what made the obvious one too slow, or what pattern this is an instance of (sliding window, two pointers, hash-map lookup, BFS, etc.).

If you tried something else first and threw it out, mention it. Future-you will thank you.

## Complexity

- **Time:** O(?) — <one-line justification>
- **Space:** O(?) — <one-line justification>

## Lessons / gotchas

Bullet anything worth remembering. Aim for 2–4 lines.

- A Go-specific thing you learned (slice header behaviour, map iteration order, zero values, etc.)
- A bug you hit and how you noticed it
- Something you'd do differently if redoing this

## Related problems

Cross-link other folders that use a similar pattern, so the repo grows into a graph instead of a pile.

- [`../<other-problem>`](../<other-problem>) — same pattern, different framing
- [`../<another>`](../<another>) — harder variant
